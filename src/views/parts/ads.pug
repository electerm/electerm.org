// Ads slot — dispatches to the active ad network configured in bin/data.js (adProvider).
// Default network is EthicalAds; set adProvider: 'carbon' to use Carbon Ads.
if adProvider === 'carbon'
  include ./carbon-ads
else
  include ./ethical-ads
